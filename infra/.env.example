# landing-production accountのAWS account ID
AWS_LANDING_PRODUCTION_ACCOUNT_ID=

# apex domain名。このリポジトリはhosted zoneを所有しないため、値は
# ACM証明書のdomainNameとCloudFront Distributionのdomainnamesにのみ使用する。
# DNSレコードの作成はaws-platformリポジトリが値の手動連携を受けて行う(ADR参照)。
APEX_DOMAIN_NAME=
